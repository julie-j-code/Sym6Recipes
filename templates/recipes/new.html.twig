{% extends 'base.html.twig' %}

{% block title %}New Recipes{% endblock %}

{% block body %}
    <h1>{{controller_name}}</h1>

    {{ include('recipes/_form.html.twig') }}

    <a class="btn btn-info my-3" href="{{ path('recipes_index') }}">back to list</a>
{% endblock %}
