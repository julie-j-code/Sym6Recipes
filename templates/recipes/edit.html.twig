{% extends 'base.html.twig' %}

{% block title %}Edit Recipes{% endblock %}

{% block body %}
    <h1>{{controller_name}}</h1>

    {{ include('recipes/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('recipes_index') }}" class="btn btn-info mt-3">Retour à la liste</a>

    {{ include('recipes/_delete_form.html.twig') }}
{% endblock %}
